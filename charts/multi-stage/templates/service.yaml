apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-stage.fullname" . }}
  labels:
    {{- include "multi-stage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-stage.selectorLabels" . | nindent 4 }}
