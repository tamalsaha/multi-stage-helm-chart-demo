apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "multi-stage.fullname" . }}-test-connection"
  labels:
    {{- include "multi-stage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "multi-stage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
